This command cannot be executed if `bd` is not in path (check `which bd`)

## ONLY IF THE ".beads" folder doesn't yet exists: 
- execute `bd init --quiet`
- Check if the ".beads" folder has been created
- execute `bd hooks install`
- Execute `bd onboard` and follow the instructions
- Report back to user
